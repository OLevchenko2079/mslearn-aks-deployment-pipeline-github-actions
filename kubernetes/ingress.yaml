apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: contoso-website
  annotations:
    kubernetes.io/ingress.class: addon-http-application-routing
spec:
  rules:
    - host: contoso.c66d99fd2aac4d69a04e.westeurope.aksapp.io
      http:
        paths:
          - backend:
              serviceName: contoso-website
              servicePort: http
            path: /
